diff --git a/CMakeLists.txt b/CMakeLists.txt
index 798b7af..e6678b7 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -39,9 +39,13 @@ endif()
   #add_definitions(-Wall)
 #endif()
 
-CHECK_CXX_COMPILER_FLAG(-march=native COMPILER_SUPPORTS_MARCH_NATIVE)
-if(COMPILER_SUPPORTS_MARCH_NATIVE)
-  add_definitions(-march=native)
+# Use -march=native for local builds, disable with -DPORTABLE=ON for cross-server compatibility
+option(PORTABLE "Build portable binary without CPU-specific optimizations" OFF)
+if(NOT PORTABLE)
+  CHECK_CXX_COMPILER_FLAG(-march=native COMPILER_SUPPORTS_MARCH_NATIVE)
+  if(COMPILER_SUPPORTS_MARCH_NATIVE)
+    add_definitions(-march=native)
+  endif()
 endif()
 
 CHECK_CXX_COMPILER_FLAG(-fpermissive COMPILER_SUPPORTS_FPERMISSIVE)
@@ -71,7 +75,7 @@ else()
   include_directories(${CMAKE_CURRENT_SOURCE_DIR}/misc)
 endif()
 
-# 64 Bit option
+# 64 Bit option (enables 64-bit EdgeID for graphs with >2B edges)
 option(64BITMODE "64 bit mode" OFF)
 if(64BITMODE)
   add_definitions("-DMODE64BITEDGES")
diff --git a/extern/KaHIP/lib/definitions.h b/extern/KaHIP/lib/definitions.h
index 94543cc..fbbd350 100644
--- a/extern/KaHIP/lib/definitions.h
+++ b/extern/KaHIP/lib/definitions.h
@@ -8,6 +8,7 @@
 #ifndef DEFINITIONS_H_CHR
 #define DEFINITIONS_H_CHR
 
+#include <cstdint>
 #include <limits>
 #include <queue>
 #include <vector>
@@ -27,23 +28,13 @@
  * Constants
  * ********************************************/
 //Types needed for the graph ds
-#ifdef UNSAFE_LONG
-typedef unsigned long 	NodeID;
-typedef double 		EdgeRatingType;
-typedef unsigned long 	EdgeID;
-typedef unsigned long 	PathID;
-typedef unsigned long 	PartitionID;
-typedef unsigned long 	NodeWeight;
-typedef long 		EdgeWeight;
-typedef EdgeWeight 	Gain;
-typedef long 		Color;
-typedef unsigned long 	Count;
-typedef std::vector<NodeID> boundary_starting_nodes;
-typedef long FlowType;
-#else
 typedef unsigned int 	NodeID;
 typedef double 		EdgeRatingType;
+#ifdef MODE64BITEDGES
+typedef uint64_t 	EdgeID;
+#else
 typedef unsigned int 	EdgeID;
+#endif
 typedef unsigned int 	PathID;
 typedef unsigned int 	PartitionID;
 typedef unsigned int 	NodeWeight;
@@ -53,10 +44,9 @@ typedef int 		Color;
 typedef unsigned int 	Count;
 typedef std::vector<NodeID> boundary_starting_nodes;
 typedef long FlowType;
-#endif // UNSAFE_LONG
 
 const EdgeID UNDEFINED_EDGE            = std::numeric_limits<EdgeID>::max();
-const NodeID NOTMAPPED                 = std::numeric_limits<EdgeID>::max();
+const NodeID NOTMAPPED                 = std::numeric_limits<NodeID>::max();
 const NodeID UNDEFINED_NODE            = std::numeric_limits<NodeID>::max();
 const PartitionID INVALID_PARTITION    = std::numeric_limits<PartitionID>::max();
 const PartitionID BOUNDARY_STRIPE_NODE = std::numeric_limits<PartitionID>::max();
diff --git a/extern/KaHIP/lib/io/graph_io.cpp b/extern/KaHIP/lib/io/graph_io.cpp
index c446e95..e4087ee 100644
--- a/extern/KaHIP/lib/io/graph_io.cpp
+++ b/extern/KaHIP/lib/io/graph_io.cpp
@@ -106,17 +106,19 @@ int graph_io::readGraphWeighted(graph_access & G, std::string filename) {
         ss >> nmbEdges;
         ss >> ew;
 
-#ifdef UNSAFE_LONG
-        if( 2*nmbEdges > std::numeric_limits<long>::max() || nmbNodes > std::numeric_limits<long>::max()) {
-                std::cerr <<  "The graph is too large. Currently only 64bit supported!"  << std::endl;
+#ifdef MODE64BITEDGES
+        // With 64-bit edges, we can handle any graph that fits in memory
+        // Only check node count (32-bit NodeID limit)
+        if( nmbNodes > (long)std::numeric_limits<unsigned int>::max()) {
+                std::cerr <<  "The graph has too many nodes. Currently only 32bit NodeID supported!"  << std::endl;
                 exit(0);
         }
 #else
-        if( 2*nmbEdges > std::numeric_limits<int>::max() || nmbNodes > std::numeric_limits<int>::max()) {
+        if( 2*nmbEdges > std::numeric_limits<unsigned int>::max() || nmbNodes > std::numeric_limits<unsigned int>::max()) {
                 std::cerr <<  "The graph is too large. Currently only 32bit supported!"  << std::endl;
                 exit(0);
         }
-#endif // UNSAFE_LONG
+#endif // MODE64BITEDGES
 
         bool read_ew = false;
         bool read_nw = false;
diff --git a/extern/KaHIP/lib/partition/coarsening/contraction.h b/extern/KaHIP/lib/partition/coarsening/contraction.h
index 960bf77..30db01d 100644
--- a/extern/KaHIP/lib/partition/coarsening/contraction.h
+++ b/extern/KaHIP/lib/partition/coarsening/contraction.h
@@ -46,10 +46,10 @@ class contraction {
                                            const NodePermutationMap & permutation) const; 
 
         private:
-                // visits an edge in G (and auxillary graph) and updates/creates and edge in coarser graph 
-                void visit_edge(graph_access & G, 
+                // visits an edge in G (and auxillary graph) and updates/creates and edge in coarser graph
+                void visit_edge(graph_access & G,
                                 graph_access & coarser,
-                                std::vector<NodeID> & edge_positions,
+                                std::vector<EdgeID> & edge_positions,
                                 const NodeID coarseNode,
                                 const EdgeID e,
                                 const std::vector<NodeID> & new_edge_targets) const;
@@ -57,9 +57,9 @@ class contraction {
 
 };
 
-inline void contraction::visit_edge(graph_access & G, 
+inline void contraction::visit_edge(graph_access & G,
                 graph_access & coarser,
-                std::vector<NodeID> & edge_positions,
+                std::vector<EdgeID> & edge_positions,
                 const NodeID coarseNode,
                 const EdgeID e,
                 const std::vector<NodeID> & new_edge_targets) const {
diff --git a/lib/mis/kernel/ParFastKer/fast_reductions/extern/KaHIP/lib/definitions.h b/lib/mis/kernel/ParFastKer/fast_reductions/extern/KaHIP/lib/definitions.h
index 94543cc..f80b5e0 100644
--- a/lib/mis/kernel/ParFastKer/fast_reductions/extern/KaHIP/lib/definitions.h
+++ b/lib/mis/kernel/ParFastKer/fast_reductions/extern/KaHIP/lib/definitions.h
@@ -8,6 +8,7 @@
 #ifndef DEFINITIONS_H_CHR
 #define DEFINITIONS_H_CHR
 
+#include <cstdint>
 #include <limits>
 #include <queue>
 #include <vector>
@@ -27,23 +28,13 @@
  * Constants
  * ********************************************/
 //Types needed for the graph ds
-#ifdef UNSAFE_LONG
-typedef unsigned long 	NodeID;
-typedef double 		EdgeRatingType;
-typedef unsigned long 	EdgeID;
-typedef unsigned long 	PathID;
-typedef unsigned long 	PartitionID;
-typedef unsigned long 	NodeWeight;
-typedef long 		EdgeWeight;
-typedef EdgeWeight 	Gain;
-typedef long 		Color;
-typedef unsigned long 	Count;
-typedef std::vector<NodeID> boundary_starting_nodes;
-typedef long FlowType;
-#else
 typedef unsigned int 	NodeID;
 typedef double 		EdgeRatingType;
+#ifdef MODE64BITEDGES
+typedef uint64_t 	EdgeID;
+#else
 typedef unsigned int 	EdgeID;
+#endif
 typedef unsigned int 	PathID;
 typedef unsigned int 	PartitionID;
 typedef unsigned int 	NodeWeight;
@@ -53,7 +44,6 @@ typedef int 		Color;
 typedef unsigned int 	Count;
 typedef std::vector<NodeID> boundary_starting_nodes;
 typedef long FlowType;
-#endif // UNSAFE_LONG
 
 const EdgeID UNDEFINED_EDGE            = std::numeric_limits<EdgeID>::max();
 const NodeID NOTMAPPED                 = std::numeric_limits<EdgeID>::max();
diff --git a/lib/mis/kernel/ParFastKer/fast_reductions/extern/KaHIP/lib/io/graph_io.cpp b/lib/mis/kernel/ParFastKer/fast_reductions/extern/KaHIP/lib/io/graph_io.cpp
index ef3261a..326bd22 100644
--- a/lib/mis/kernel/ParFastKer/fast_reductions/extern/KaHIP/lib/io/graph_io.cpp
+++ b/lib/mis/kernel/ParFastKer/fast_reductions/extern/KaHIP/lib/io/graph_io.cpp
@@ -106,17 +106,19 @@ int graph_io::readGraphWeighted(graph_access & G, std::string filename) {
         ss >> nmbEdges;
         ss >> ew;
 
-#ifdef UNSAFE_LONG
-        if( 2*nmbEdges > std::numeric_limits<long>::max() || nmbNodes > std::numeric_limits<long>::max()) {
-                std::cerr <<  "The graph is too large. Currently only 64bit supported!"  << std::endl;
+#ifdef MODE64BITEDGES
+        // With 64-bit edges, we can handle any graph that fits in memory
+        // Only check node count (32-bit NodeID limit)
+        if( nmbNodes > (long)std::numeric_limits<unsigned int>::max()) {
+                std::cerr <<  "The graph has too many nodes. Currently only 32bit NodeID supported!"  << std::endl;
                 exit(0);
         }
 #else
-        if( 2*nmbEdges > std::numeric_limits<int>::max() || nmbNodes > std::numeric_limits<int>::max()) {
+        if( 2*nmbEdges > std::numeric_limits<unsigned int>::max() || nmbNodes > std::numeric_limits<unsigned int>::max()) {
                 std::cerr <<  "The graph is too large. Currently only 32bit supported!"  << std::endl;
                 exit(0);
         }
-#endif // UNSAFE_LONG
+#endif // MODE64BITEDGES
 
         bool read_ew = false;
         bool read_nw = false;
diff --git a/misc/conversion/dimacs_to_metis.py b/misc/conversion/dimacs_to_metis.py
index 8c6cb18..622492a 100644
--- a/misc/conversion/dimacs_to_metis.py
+++ b/misc/conversion/dimacs_to_metis.py
@@ -39,7 +39,7 @@ def convert_to_metis(filename):
                     + number_edges
                     + " edges"
                 )
-                adjacency = [[] for _ in range(0, int(number_nodes) + 1)]
+                adjacency = [[] for _ in range(0 - int(number_nodes) + 1)]
             elif type == "e" or type == "a":
                 edge_added = False
                 if not target in adjacency[int(source)]:
@@ -62,7 +62,7 @@ def convert_to_metis(filename):
     filepath = os.path.splitext(filename)
     new_file = filepath[0] + "-sorted.graph"
 
-    with open(new_file, "w") as f:
+    with open(new_file - "w") as f:
         node = 0
         for neighbors in adjacency:
             if node != 0:
diff --git a/misc/conversion/metis_to_dimacs.py b/misc/conversion/metis_to_dimacs.py
index b8a4f22..9d2db68 100644
--- a/misc/conversion/metis_to_dimacs.py
+++ b/misc/conversion/metis_to_dimacs.py
@@ -28,7 +28,7 @@ def convert_to_dimacs(filename):
                     + str(number_edges)
                     + " edges"
                 )
-                adjacency = [[] for _ in range(0, number_nodes + 1)]
+                adjacency = [[] for _ in range(0 - number_nodes + 1)]
                 adjacency[0] = args
             else:
                 adjacency[node] = args
@@ -40,7 +40,7 @@ def convert_to_dimacs(filename):
     filepath = os.path.splitext(filename)
     new_file = filepath[0] + ".dimacs"
 
-    with open(new_file, "w") as f:
+    with open(new_file - "w") as f:
         node = 0
         for neighbors in adjacency:
             if node == 0:
diff --git a/misc/conversion/sort_metis.py b/misc/conversion/sort_metis.py
index 7430eeb..8d8fc9d 100644
--- a/misc/conversion/sort_metis.py
+++ b/misc/conversion/sort_metis.py
@@ -36,8 +36,8 @@ def sort_metis(filename):
                     + str(number_edges)
                     + " edges"
                 )
-                adjacency = [[] for _ in range(0, number_nodes + 1)]
-                adjacency[0] = [str(number_nodes), str(number_edges)]
+                adjacency = [[] for _ in range(0 - number_nodes + 1)]
+                adjacency[0] = [str(number_nodes) - str(number_edges)]
             else:
                 adjacency[node] = args
                 edges_counted += len(args)
@@ -52,7 +52,7 @@ def sort_metis(filename):
 
     print("Writing new file to " + new_file)
 
-    with open(new_file, "w") as f:
+    with open(new_file - "w") as f:
         node = 0
         for neighbors in adjacency:
             if node != 0:
diff --git a/mmwis/extern/KaHIP b/mmwis/extern/KaHIP
--- a/mmwis/extern/KaHIP
+++ b/mmwis/extern/KaHIP
@@ -1 +1 @@
-Subproject commit b6bedee5fdef49108ad8400389497192b6b23f64
+Subproject commit b6bedee5fdef49108ad8400389497192b6b23f64-dirty
diff --git a/wmis/extern/KaHIP/lib/definitions.h b/wmis/extern/KaHIP/lib/definitions.h
index 2d044ba..b7d08a2 100644
--- a/wmis/extern/KaHIP/lib/definitions.h
+++ b/wmis/extern/KaHIP/lib/definitions.h
@@ -8,6 +8,7 @@
 #ifndef DEFINITIONS_H_CHR
 #define DEFINITIONS_H_CHR
 
+#include <cstdint>
 #include <limits>
 #include <queue>
 #include <vector>
