<thought>
Baseline implementation that assigns equal priority to all nodes.
</thought>

<code>
def priority(node, G, node_to_vertex, vertex_to_node, n, s, q):
    """Returns the priority with which we want to add `node` to the independent set.
    {score}

    Args:
        node: string of length n with symbols from {0, 1, ..., q-1}
        G: graph-tool Graph object (read-only)
        node_to_vertex: dict mapping node strings to vertex indices
        vertex_to_node: dict mapping vertex indices to node strings
        n: length of the string
        s: number of errors (insertions/deletions/substitutions) the code can correct
        q: alphabet size
    """
    return 0.0
</code>
