<thought>
Baseline implementation that assigns equal priority to all nodes.
</thought>

<code>
def priority(node, G, node_to_vertex, vertex_to_node, n, s) -> float:
    """Returns the priority with which we want to add `node` to the independent set.{score}"""
    return 0.0
</code>
