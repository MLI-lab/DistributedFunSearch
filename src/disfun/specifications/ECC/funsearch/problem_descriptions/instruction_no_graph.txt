I need help designing a priority function for constructing large {error_type}-correcting codes.

I construct codes greedily by:
1. Compute priority(codeword) for all possible {string_type} strings of length n
2. Select the highest-priority codeword, add it to the code, and remove it from the candidates
3. Remove all candidates that share a common subsequence of length at least n-s with the selected codeword (where s is the number of deletions to correct), as these cannot be in the same code
4. Repeat until no candidates remain

A better priority function produces a larger code.

Consider properties of the {string_type} string, such as specific patterns and the number of ones and zeros.
